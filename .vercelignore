# Trim the Vercel upload (the first deploy shipped ~519 MB). Vercel reinstalls deps
# and rebuilds remotely, so none of these are needed in the upload.
node_modules
.next
.git



# Python pipeline inputs / scratch — never imported or served by the app.
source
