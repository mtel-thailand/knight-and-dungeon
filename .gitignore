# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
/build

# typescript build info
*.tsbuildinfo
next-env.d.ts

# local SQLite database (legacy — no longer used)
/data/app.db
/data/app.db-shm
/data/app.db-wal

# production env (never commit — contains DB credentials)
.env.prod

# misc
.DS_Store
*.pem

# firebase / gcp service-account credentials (never commit)
*-firebase-adminsdk-*.json
serviceAccount*.json
firebase-service-account*.json

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# env
.env
.env*.local
.vercel

# deepwork session state (local, not committed)
.slim/deepwork/
